import { useContext } from "react";
import { AuthContextProvider, UserAuth } from "../contexts/AuthContext";

const Home = () => {
    return (
        <div>
            <h1>Hello</h1>
        </div>
    )
}

export default Home;